function createAnimation(options) {
  var from = options.from;
  var to = options.to;
  var duration = options.duration || 1000;
  var delay = options.delay || 10;
    var easing = duration / delay;
    var 
}
